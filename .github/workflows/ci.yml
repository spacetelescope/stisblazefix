name: stisblazefix CI

on: [push, pull_request]

jobs:

  #environment-installation:
  #
  #  runs-on: ${{ matrix.os }}
  #  strategy:
  #    matrix:
  #      os: [ubuntu-latest, macos-latest]
  #      python-version: [3.7, 3.8]
  #
  #  steps:
  #    - uses: actions/checkout@v2
  #    - name: Test stisblazefix environment installation
  #      run: |
  #        cd $RUNNER_WORKSPACE
  #        sudo conda create --yes --prefix ./envs python=3.7
  #        eval "$(${CONDA}/bin/conda shell.bash hook)"
  #        source activate ./envs
  #        cd stisblazefix/
  #        sudo conda env update -f doc/environment.yml
  #        conda activate stisblazefix
  #        pip install -e .
  #        python -c "import stisblazefix"
  #      shell: bash

  unit-tests:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]  #, macos-latest]
        python-version: [3.8]  #, 3.7]

    steps:
      - uses: actions/checkout@v2
      - uses: conda-incubator/setup-miniconda@v2
        with:
          activate-environment: stisblazefix-env
          environment-file: doc/environment.yml
          auto-activate-base: false
          auto-update-conda: true
          python-version: ${{ matrix.python-version }}
          channels: conda-forge
          allow-softlinks: true
          channel-priority: flexible
          show-channel-urls: true
      - name: conda info
        shell: bash -l {0}
        run: |
          conda info
          conda list

      #- name: Run stisblazefix unit tests
      #  with:
      #    python-version: ${{ matrix.python-version }}
      #  run: |
      #    cd $RUNNER_WORKSPACE
      #    sudo conda create --yes --prefix ./envs python=${{ matrix.python-version }}
      #    eval "$(${CONDA}/bin/conda shell.bash hook)"
      #    source activate ./envs
      #    cd stisblazefix/
      #    sudo conda env update -f doc/environment.yml
      #    conda activate stisblazefix
      #    pip install -e .
      #    pytest stisblazefix/
      #  shell: bash
